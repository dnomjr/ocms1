# ===================================
#  Form Field Definitions
# ===================================

fields:
    date:
        label: Actual time & date
        type: text
        readOnly: true
        default: '{{ Arrivals.getCurrentTime() }}'

    id:
        label: ID
        disabled: true
        
    name:
         label: Student name 