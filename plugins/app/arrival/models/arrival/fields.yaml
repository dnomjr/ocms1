# ===================================
#  Form Field Definitions
# ===================================

fields:
    date:
        label: Actual time & date
        type: text
        readOnly: true
        default: '{{ Plugin.getCurrentTime() }}'

    id:
        label: ID
        disabled: true
        
    name:
         label: Student name 